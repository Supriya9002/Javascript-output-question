function isStringCreated(str1, str2) {
  if (str1.length !== str2.length) {
    return false;
  }
  let freq = str1;
  for()
}

console.log(isStringCreated("anagram", "nagaram"));

// "aaz","zza" ==> false
// "qwerty","qeywrt" ==> true

let a = "zza";
// Objects or Arrays, that time b also change
let b = a; //[...a].join("");
b = 123;
console.log(a, b);
